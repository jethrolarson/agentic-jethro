# Provenance

Maintainer metadata for the specimens cited in this skill — when observed, on what, when last reverified. Executing agents don't need this file; it exists so entries can be confidently re-dated or deleted instead of accumulating (see `distilling-from-failure.md`).

## 200-reference-file skill rot

*Cited in:* `SKILL.md` (size warning)

- **Observed:** ~2026-07-06, in a private work codebase. Details deliberately withheld; the specimen is testimony-only and cannot be re-examined from this repo.
- **What was observed:** a skill whose ~200 reference files were generated without anyone reading the output back — incoherent, redundant, unpruned (see `docs/inception.md` for the contemporaneous account).
- **Reverification:** not independently possible; ask Jethro. If nobody can vouch for it anymore, the SKILL.md claim should soften from "we watched" to a hypothetical, or the accumulation mechanism should stand on newer evidence.
- **Last reverified:** 2026-07-11 (Jethro, from memory).

## Fabricated justifications under pressure to justify

*Cited in:* `references/authoring-new.md` (fix given without its failure)

- **Observed:** 2026-07-11, validation probe 2 (`docs/validation.md`): an agent given only the prompting philosophy and a bare-rule skill with an unreachable author invented incident history ("learned the hard way") inside the improved artifact, confessing only in commentary its readers would never see.
- **Reverification:** fully reproducible — rerun the probe per `docs/validation.md`.
- **Last reverified:** 2026-07-11 (original run, claude-fable-5).

## Payments-service example

The 👎/✅ block in `SKILL.md` is an illustrative exhibit demonstrating the hazard-entry form, not an observed incident (constructed during the inception conversation, `docs/inception.md`). It owes no provenance; it owes only not being mistaken for history, which its "Hazard-driven:" example framing carries.
