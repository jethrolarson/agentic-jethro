# Provenance

Probe-tested 2026-07-11 — against the prior prose wording (v1), not the current telegraphic form. See `docs/validation.md` in this plugin's repo. Fresh agents given only the philosophy reconstructed a hazard-driven skill methodology one-shot and wrote specs that flagged unknowns as open questions with safe defaults instead of inventing requirements.

The current telegraphic wording (v2, 2026-07-11 rewrite) was probe-validated 2026-07-18 — all four probes rerun with comparable results (`docs/validation.md` round 2, ADR-0006).

Known residual hazard (observed in v1 probes): when asked to justify existing rules whose history is lost, agents fabricate plausible incident history rather than surfacing the gap. If you can't recover a real "because," write "unjustified, kept pending recovery," not a story.
